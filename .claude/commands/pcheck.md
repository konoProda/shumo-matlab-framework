---
description: 论文交付前自检：篇幅/图表引用/数字一致性/去AI复查/反抄袭/占位符/可打包性
---
# /pcheck - 论文交付前检查

## 触发条件
全部章节撰写完成后调用，或用户输入 `/pcheck`。

## 执行流程
1. **确定题目**：支持显式参数；未指定时读取 `shumo/.active_problem.txt`。
2. **强制加载规范**：先 Read `paper-spec`、`paper-writer`、`paper-format`、`humanizer` 四个 SKILL.md。
3. **逐项检查（paper-spec §九清单）**：
   - [ ] 篇幅：估算总页数（正文约每 400~450 汉字/页 + 图表占页），是否在 25~30 页；
   - [ ] 图表引用：figures/ 中每张被引用图是否在正文中出现且图前有引导句、图后有解释句；
   - [ ] **数字一致性【关键确认点 #3】**：正文全部数字与 `PAPER_HANDOFF.md` / `outputs/` 核对（逐条列差异）；
         须产出完整核查表（模板见 `_CHECK_TEMPLATES.md` 模板3）并**停止等待编程/论文手确认**，不得作为非阻塞自检略过；
         **同时生成 `<题目名>/建模手交接/交付建模手/致建模手_确认点3.md`**（2026-09-11 新增，见 CLAUDE.md §确认点交付单；
         确认点为全局，不加 `_qX`）：列出需建模手/论文手回复的少数项 + 仅告知项 + 填写式回复区；
   - [ ] **风险口径落地**：`IMPLEMENTATION_REPORT.md` §已知警告与限制 中的每条风险，
         须在论文对应位置如实体现（该报告含"风险→论文对接表"，逐条核对不得遗漏或淡化）；
   - [ ] 去 AI 复查：按 humanizer 清单再扫一遍（重点：破折号、AI 高频词、三连排比、空洞结论）；
   - [ ] 反抄袭：与参考论文的表述相似度自查（标题句式/过渡语/解释措辞），相似处列出并改写；
   - [ ] 占位符：个人信息是否仍为【待填】；
   - [ ] LaTeX 语法：环境配对（\begin/\end）、$ 配对、特殊字符转义（_ & % #）、\input 路径存在；
   - [ ] 标题编号：\section/\subsection 标题中无手写序号（无双重编号）；附录小节用带星命令；
   - [ ] 摘要页：占且仅占一页（\newpage 在关键词后、大标题紧凑块、无 abstract 环境缩进）；
   - [ ] 图内标题：无图号前缀；饼图/柱状图带数值标注且与正文数字一致；
   - [ ] 可打包性：paper/ 自包含（figures 已复制、无绝对路径引用）；latexmkrc 与 README 就位；
4. **输出检查报告**：逐项 PASS/FAIL，FAIL 项给出具体位置与修复建议。
5. **修复**：FAIL 项在征得编程手同意后修复，或由编程手决定保留。
6. **收尾**：更新 `.paper_progress.txt`（标记 completed），给出 README 中编译命令提醒。

## 注意
本机无 LaTeX 环境：无法实际编译，检查以静态语法核对为准；编译报错请用户将日志发回，我协助排错。

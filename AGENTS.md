# Codex 工作指引

本仓库是数学建模竞赛工作区。Codex 以本文件为主，`.claude/` 目录仅保留兼容历史流程，不作为 Codex 的主入口。

## 基本约束

- 仅在本仓库内读写和运行文件，默认工作路径为 `D:\cc\shumo-matlab-framework`。
- 默认用中文沟通。
- 不主动涉及仓库外文件。
- 保留用户已有改动，不做无关重构。
- 需要改文件时优先使用 `apply_patch`。
- 不使用破坏性命令，不擅自清理用户数据。

## 目录约定

每个题目单独放在 `<题目名>/` 下，常见结构如下：

- `data/`：题目数据
- `src/`：MATLAB 主程序与函数
- `scripts/`：辅助脚本
- `tests/`：测试脚本
- `outputs/`：运行结果、日志、失败说明
- `figures/`：图表
- `paper/`：论文 LaTeX 工程
- `IMPLEMENTATION_REPORT.md`：实现验证报告
- `PAPER_HANDOFF.md`：交给论文写作侧的总结文档

当前题目状态记录在 `.active_problem.txt`，格式为 `<题目名>|<Phase>`。如果状态为空且用户没有明确题目名，先询问题目名，不要猜。

## 代码工作流

对建模代码任务，按下面顺序推进：

### Phase 0：`/prep`

- 先确认或创建题目目录和标准子目录。
- 读取题目数据，梳理数学模型，建立“数学符号 → 代码变量 → 维度”的映射。
- 数据缺失时先向用户询问生成规则，不擅自假设分布或参数。
- 数据格式不符时直接指出期望格式与实际格式，不静默补零或丢行。
- 完成后停止，等待用户确认后再进入下一阶段。

### Phase 1：`/code`

- 基于确认后的映射生成 MATLAB 主程序和必要子函数。
- 主程序统一使用 `PROJ_ROOT = fullfile(fileparts(mfilename('fullpath')), '..')`。
- 注释只保留与公式编号、符号对应的必要信息。
- 先生成主程序，再生成子函数。
- 生成后停止，等待用户确认。

### Phase 2：`/test`

- 在 `tests/` 下生成独立测试脚本。
- 重点验证维度、特殊值、稳定性、收敛性、参考解一致性。
- 测试结果写入 `outputs/test_results.mat` 和 `outputs/test_log.txt`。
- 失败时写入 `outputs/failure_notes.md`，并停止流转，请用户介入。

### Phase 3：`/report`

- 用完整原始数据重新运行主程序。
- 结果写入 `outputs/final_results.mat`，图表写入 `figures/`。
- 生成 `IMPLEMENTATION_REPORT.md`，包含执行摘要、映射表、运行记录、测试结论和风险提示。

## 论文工作流

论文任务只在代码验证完成后推进。

- 先生成 `PAPER_HANDOFF.md`，再进入论文写作。
- 论文工程放在 `<题目名>/paper/`，保持自包含，可单独打包。
- 章节按顺序逐章写，每章完成后必须停下并等待用户确认。
- 所有数字、图表、结论只允许来自 `PAPER_HANDOFF.md`、`outputs/` 和 `figures/`。
- 不虚构数据、结果、参考文献或图表结论。

写作与排版遵循这些原则：

- 摘要只占一页，正文控制在 25~30 页。
- 图表必须在正文中被引用，并且图前有引导、图后有解释。
- 章节标题、公式编号、图表编号统一。
- 去除明显 AI 腔，避免空泛套话。
- 个人信息一律保留占位符，提交前再补。

## 质量要求

- MATLAB 代码保持主程序 + 子函数的拆分。
- 优先保证路径稳定、结果可复现、变量命名清晰。
- 任何阶段如果信息不足，就先提问，不要猜。
- 完成后给出简短、明确的变更摘要和验证结果。

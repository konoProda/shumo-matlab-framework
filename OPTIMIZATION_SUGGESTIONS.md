# 体系优化建议报告

> 审查对象：`CLAUDE.md` + `.claude/commands/{prep,code,test,report}.md`
> 审查身份：Claude Code 体系审阅专家
> 审查日期：2026-08-13
> 环境实测：Linux 虚拟机，MATLAB R2026a（Update 4）位于 `/home/sck/matlab/bin/matlab`，`matlab -batch` 无头模式实测可用；Octave 未安装。

---

## 一、裁决确认：报告文件名统一

已正式裁决统一为 **IMPLEMENTATION_REPORT.md**，修正动作已完成并 grep 校验：

| 位置 | 状态 |
|------|------|
| CLAUDE.md「全局目录规则」 | ✅ 本次已修正（原为 REPORT.md） |
| CLAUDE.md Phase 3 描述 | ✅ 原本即为 IMPLEMENTATION_REPORT.md，无需修改 |
| .claude/commands/report.md（两处引用） | ✅ 原本即为 IMPLEMENTATION_REPORT.md，无需修改 |

全仓已无裸 `REPORT.md` 引用。

---

## 二、工作流整体适配性

### 1.1 跨目录路径处理 —— 缺少"当前活跃题目"状态机制（必须改）

**问题**：
Claude 在 `shumo/` 启动，但所有文件操作需落到 `shumo/<题目名>/`。当前规则完全依赖**对话记忆**维持目标题目——一旦开启新会话、上下文被压缩、或用户跳过 /prep 直接说"帮我写代码"，Claude 无法确定操作目标，存在把文件误写到 `shumo/` 顶层或其他题目目录的风险。

**建议**：
引入轻量状态文件 `shumo/.active_problem.txt`（配置层），内容格式 `<题目名>|<当前Phase>`。规则要点：
- /prep 结束时由 Claude 写入/更新；
- /code、/test、/report 执行前**必须先读取**该文件确认操作目标；
- 任何文件写入前，先在回复中输出目标路径 `shumo/<题目名>/...`；
- 状态文件为空且用户未指明题目名时，必须先询问题目名，**禁止猜测**。

**修改后原文**（加入 CLAUDE.md「全局目录规则」）：
```markdown
- **当前工作题目状态**：`shumo/.active_problem.txt` 记录当前活跃题目与所处阶段（格式：`<题目名>|<当前Phase>`）。/prep 结束时由 Claude 写入/更新；/code、/test、/report 执行前必须先读取该文件确认操作目标，并在回复中先输出目标路径 `shumo/<题目名>/...`。若状态文件为空且用户未指明题目名，必须先询问题目名，禁止猜测或写入 shumo/ 顶层。
```

### 1.2 多题目并行管理 —— 命令不支持题目名参数（建议改）

**问题**：
当 `shumo/` 下存在多个题目目录（如 `A题/`、`B题/`、`2026C/`）时，/code、/test、/report 均不接受题目名参数，仅靠对话上下文区分。跨会话或并行处理多题时极易写错目录。

**建议**：
- /prep 阶段**强制要求**输入题目名（不可省略、不可由 Claude 猜测默认值）；
- /code、/test、/report 支持显式参数：`/test A题`，**参数优先级高于状态文件**；
- 执行任何文件写入前，先在回复中列出目标目录树供核对。

**修改后原文**（加入 code.md / test.md / report.md 的触发条件节）：
```markdown
## 触发条件
- 支持显式指定题目：`/code <题目名>`（如 `/code A题`）；未指定时读取 `shumo/.active_problem.txt` 中的当前题目。
- 状态文件为空且未指定题目名时，先询问题目名，不得默认猜测。
```

### 1.3 自动流转 vs 手动干预 —— 失败后无重试机制（必须改）

**问题**：
/test 失败时规则仅说"报错并请求介入"，未定义重试机制：用户修复代码后，不确定"重新输入 /test 是否有效、是否需要重跑 /prep 与 /code"。失败后的流转状态也无人记录，多轮修复时信息丢失。

**建议**：
- 定义 /test 为**幂等操作**（每次运行覆盖旧结果），失败后可反复重跑，无需重跑 /prep 与 /code；
- 失败时自动写入 `<题目名>/outputs/failure_notes.md`（失败项 + 预期 vs 实际 + 复现命令），作为修复与重测的输入；
- 不新增 /retest 命令（避免命令膨胀），在 /test 中说明重跑规则即可。

**修改后原文**（test.md「判定」节）：
```markdown
## 判定
- 所有测试项通过 → 自动进入 /report。
- 任一测试项不通过 → 停止流转，报错（含失败项、预期值 vs 实际值、复现命令），并将失败详情写入 `<题目名>/outputs/failure_notes.md`，请求编程手介入。
- 重试机制：编程手修复后再次输入 `/test` 即可（幂等覆盖旧结果），无需重跑 /prep 与 /code。
```

---

## 三、Command 文件审视

### 2.1 prep.md

#### 2.1-a 解析步骤无固定模板（建议改）

**问题**：
仅写"解析建模手提供的数学模型"，未给出解析步骤模板。每次执行质量取决于临场发挥，映射表结构可能不稳定，/code 阶段难以直接消费。

**建议**：
固化为 5 步模板：变量清单 → 目标函数 → 约束 → 数据 I/O 规格 → 歧义清单。

**修改后原文**（prep.md「目标」节）：
```markdown
## 解析步骤（固定模板）
1. **变量清单**：所有数学符号 → 含义 → 数据类型/维度 → 建议代码变量名。
2. **目标函数**：完整表达式 + 优化方向（min/max）。
3. **约束条件**：逐条编号（等式/不等式/上下界），标注公式编号。
4. **数据 I/O 规格**：输入文件名、格式（.xlsx/.csv/.mat）、行列含义；输出维度与含义。
5. **歧义与缺失清单**：逐条编号列出不确定处，向编程手提问。
```

#### 2.1-b 模拟数据生成规则不明确（必须改）

**问题**：
"若无数据则生成模拟数据"缺少两个关键定义：**如何判定"数据缺失"**、**生成规则由谁提供**。当前写法可能诱导 Claude 凭空假设数据分布（如默认正态分布），模拟数据污染后续全部验证而不自知。

**建议**：
- 判定缺失的条件：`<题目名>/data/` 下无题目所给文件，**或**用户明确声明"无官方数据"；
- 生成规则（变量维度、分布类型、参数范围、样本量）**必须由编程手/建模手提供**；用户未提供时 Claude 不得生成，而是列出所需信息清单提问；
- 模拟数据仅用于流程验证，日志与报告中必须标注"模拟数据"。

**修改后原文**（prep.md「目标」节第 2 条）：
```markdown
2. 检查 `<题目名>/data/` 下是否存在题目所给数据文件。判定为缺失的条件：目录下无相关文件，或用户明确声明"无官方数据"。若缺失，向编程手询问模拟数据生成规则（变量维度、分布类型、参数范围、样本量）——规则必须由编程手提供，Claude 不得擅自假设分布；生成后执行预处理（标准化/正则化），并在日志与报告中标注"模拟数据"。
```

#### 2.1-c 映射表结构化程度不足（可选优化）

**问题**：
现有 4 列（数学符号 | 含义 | 数据类型/维度 | 建议代码变量名）缺少"公式编号"与"归属（目标/约束）"，且仅存 .md——新会话中上下文被压缩时，/code 阶段可能丢失映射细节。

**建议**：
映射表增为 5 列；同时另存机器可读副本 `<题目名>/outputs/math_to_code_mapping.json`，供 /code 阶段直接读取，防止跨会话上下文丢失。

**修改后原文**（prep.md「目标」节第 3 条 + 「输出产物」节）：
```markdown
3. 映射表格式（5 列）：公式编号 | 数学符号 | 含义 | 数据类型/维度 | 代码变量名；每个符号标注归属（目标函数/约束 i）。
```
```markdown
- 结构化副本保存为 `<题目名>/outputs/math_to_code_mapping.json`，供 /code 直接读取。
```

### 2.2 code.md

#### 2.2-a 致命错误检查清单不完整（建议改）

**问题**：
现清单仅 3 项（矩阵维度、while 终止、递归出口），漏掉多个 MATLAB 高发陷阱。

**建议**：
扩充清单：`.*` 与 `*`、`'` 与 `.'`（复数转置陷阱）、broadcast 维度兼容、for 循环内修改迭代变量、while 最大迭代护栏、size/length/numel 混用（length 对非向量矩阵返回 max(size)）、除零、索引越界（end+1）。

**修改后原文**（code.md「执行流程」第 1 条）：
```markdown
1. 致命错误检查清单：
   - 矩阵维度：`*` 与 `.*`、`'` 与 `.'`（复数转置）、broadcast 兼容性；
   - 循环：while 必须有终止条件 + 最大迭代次数护栏；for 循环内禁止修改迭代变量；
   - 递归：必须有出口条件；
   - 维度函数：size/length/numel 语义正确（length 对非向量矩阵返回 max(size)）；
   - 数值风险：除零、索引越界（end+1）、整型截断。
```

#### 2.2-b 数据路径依赖 pwd 脆弱（建议改）

**问题**：
`fullfile(pwd,'data',...)` 依赖 MATLAB **启动目录**恰好为 `<题目名>/`。用户若在 `shumo/` 或 `src/` 下启动 MATLAB 运行脚本，路径静默失败——更糟的是，若 `shumo/` 顶层恰有同名 data/ 目录，会**读到错误数据且不报错**。

**建议**：
保留 fullfile 风格，但根目录改为基于脚本自身位置的定位：`PROJ_ROOT = fullfile(fileparts(mfilename('fullpath')), '..')`，之后所有路径用 `fullfile(PROJ_ROOT, 'data', ...)`。脚本无论从何处启动均正确。若坚持 pwd 方案，至少加断言：`assert(isfolder(fullfile(pwd,'data')), '请在 <题目名>/ 目录下运行')`。

**修改后原文**（code.md「执行流程」第 3 条）：
```markdown
3. 数据读取路径：主程序顶部统一定义
   `PROJ_ROOT = fullfile(fileparts(mfilename('fullpath')), '..');  % 题目根目录（脚本位于 <题目名>/src/）`
   后续一律 `fullfile(PROJ_ROOT, 'data', '文件名')`，保证脚本从任意目录启动均可正确读取（比 fullfile(pwd,'data',...) 更健壮，且保持同风格）。
```

#### 2.2-c 无同名文件覆盖保护（建议改）

**问题**：
未定义 `<题目名>/src/` 已存在同名 .m 文件时的处理。反复迭代调参时，可能静默覆盖用户手工修改过的版本。

**建议**：
生成前检查同名文件；存在时先备份为 `<文件名>.m.bak_<时间戳>`；写入前在回复中列出"本次将创建/覆盖的文件清单"。

**修改后原文**（code.md「执行流程」新增第 0 条）：
```markdown
0. 覆盖检查：若 `<题目名>/src/`（或 scripts/、tests/）已存在同名 .m 文件，先备份为 `*.m.bak_<时间戳>`；在回复中列出"本次将创建/覆盖的文件清单"后再写入。
```

### 2.3 test.md

#### 2.3-a 特殊值检验定义不具体（建议改）

**问题**：
仅写"特殊值检验（如零向量、单位向量）"，边界值、退化输入、解析解用例未定义，执行时覆盖面随缘，检验形同虚设。

**建议**：
给出固定候选清单（按模型适用性勾选），每项须有**可手算的预期输出**，输出与手算值相对误差 < 1e-6 为通过。

**修改后原文**（test.md 测试项）：
```markdown
- **特殊值检验**（按适用性勾选，每项须有可手算的预期输出）：
  ① 零向量/零矩阵（输出应为 0 或按公式定义值）；
  ② 单位向量/单位矩阵；
  ③ 变量上下界边界值；
  ④ 退化输入（重复行、奇异矩阵）；
  ⑤ 已知解析解用例（由建模手提供，相对误差 < 1e-6 为通过）。
```

#### 2.3-b 稳定性阈值未按算法类型分层（必须改）

**问题**：
固定 `std < 1e-3` 对确定性算法合理，但对遗传算法、模拟退火、粒子群等随机算法**必然误报**——同参数两次运行的最优值差异可达数个百分点，1e-3 阈值在物理上不可达。

**建议**：
按算法类型分层判定：确定性算法 vs 随机/启发式算法。

**修改后原文**（test.md 测试项）：
```markdown
- **稳定性检验**：重复运行 10 次。确定性算法：输出标准差 < 1e-3 且相对标准差（std/|mean|）< 1%；随机/启发式算法（遗传算法、模拟退火、粒子群等）：多次运行最优值的相对波动 < 5% 且收敛水平一致为稳定。
```

#### 2.3-c 失败报错缺对比数据（必须改）

**问题**：
"报错并请求介入"未定义报错内容。用户拿到"失败"二字无法定位问题，需要重新提问、来回消耗时间——竞赛场景下不可接受。

**建议**：
失败报错固定包含：失败测试项、输入摘要、预期值、实际值、相对误差、复现命令、日志路径。

**修改后原文**（test.md「判定」节）：
```markdown
- 任一测试项不通过 → 报错格式固定为：
  | 失败项 | 输入摘要 | 预期值 | 实际值 | 相对误差 |
  并附复现命令（如 `matlab -batch "run('<题目名>/tests/test_<模型名>.m')"`）与日志路径，请求编程手介入。
```

### 2.4 report.md

#### 2.4-a 报告缺执行摘要（建议改）

**问题**：
四个章节直接从映射表开始，建模手无法一眼看到"验证通过没有"。竞赛场景下建模手时间紧张，需要 10 秒内得出结论。

**建议**：
报告开头增加"〇、执行摘要"：一句话结论（全部测试通过/存在失败项）+ 三个关键指标（总耗时、稳定性、最大误差）。

**修改后原文**（report.md 报告结构节）：
```markdown
## 实现验证报告 (`<题目名>/IMPLEMENTATION_REPORT.md`)
结构如下：
- 〇、执行摘要：一句话结论（全部测试通过/存在失败项）+ 关键指标（总耗时、稳定性、最大误差）。
- 一、数学→代码映射表
- 二、运行记录（数据规模、耗时、内存）
- 三、测试验证结论（各测试项结果）
- 四、代码质量与风险提示
```

#### 2.4-b 向量化程度不可量化（可选优化）

**问题**：
"向量化程度：高/中/低"无判定标准，同一代码在不同会话中可能给出不同评级，报告失去可比性。

**建议**：
量化分级：统计 src/ 计算主体代码中显式 for/while 循环体数量：≤2 → 高；3–5 → 中；>5 → 低，低评级时列出最耗时循环的文件与行号。

**修改后原文**（report.md 第四章）：
```markdown
- 向量化程度：按"src/ 计算主体中显式 for/while 循环体数量"分级：≤2 → 高；3–5 → 中；>5 → 低（须列出最耗时循环的文件与行号）。
```

---

## 四、CLAUDE.md 规则审视

### 3.1 规则一致性（必须改-措辞统一）

**问题**：
- ① 全局规则 Phase 0 写"标准化/正则化处理"，prep.md 只写"预处理"，措辞不一致；
- ② REPORT.md 与 IMPLEMENTATION_REPORT.md 冲突（**已裁决修复**，见第一节）；
- ③ 其余 Phase 描述与四个命令文件内容一致，无矛盾；
- ④ CO-STAR 部分 Objective"禁止擅自修改建模逻辑"与 Phase 1"不修改建模逻辑"表述一致，无需改动。

**建议**：
统一措辞为"预处理（标准化/正则化）"，CLAUDE.md Phase 0 与 prep.md 两处同步修改（见 2.1-b 修改后原文）。

### 3.2 指令清晰度

#### 3.2-a "确认"语义未定义（建议改）

**问题**：
"必须等待编程手明确回复'确认'或'同意'"——若用户回复"行"、"没问题"、"确认，但把 x1 改为向量"，Claude 无法判定是否算确认。语义不清会导致两个方向的事故：过早进入 Phase 1（修改意见被吞），或反复追问惹恼用户。

**建议**：
定义确认语义：回复含肯定词（确认/同意/OK/没问题/行）**且不含修改意见** → 视为确认；含修改意见 → 不视为确认，先更新映射表并再次询问。无论哪种情况，进入 Phase 1 前必须在对话中复述最终版映射表。

**修改后原文**（CLAUDE.md Phase 0 末条）：
```markdown
- 必须等待编程手明确回复确认后方可进入 Phase 1。确认语义：回复含肯定词（确认/同意/OK/没问题）且不含修改意见；若回复含修改意见，Claude 先更新映射表并重新询问确认。进入 Phase 1 前必须在对话中复述最终版映射表。
```

#### 3.2-b "根据题目描述生成模拟数据"来源模糊（必须改）

**问题**：
Claude 无法主动获取"题目描述"——这条规则的执行路径不存在。若建模手文档已在对话中提供则成立；否则 Claude 会卡住或臆造数据规格。

**建议**：
明确：数据规格（维度、格式、量级）由编程手在 Phase 0 对话中提供；缺失时 Claude 输出"模拟数据所需信息清单"提问，而非自行假设。与 2.1-b 合并落地，CLAUDE.md Phase 0 数据检查条同步修改。

### 3.3 边界情况处理

#### 3.3-a 用户中途手动修改代码未定义（建议改）

**问题**：
Phase 1 生成代码后、Phase 2 测试前，用户可能手工调参或改逻辑。当前规则未定义检测与应对，测试结果可能不再对应映射表，"验证一致性"名存实亡。

**建议**：
/test 开始前对比 src/ 下各 .m 文件修改时间与 Phase 1 生成时间；发现人工修改的文件，在日志与报告中标注"人工修改文件：xxx"，并对该文件逐条复查映射；测试结论区分"通过（含人工修改）"与"通过（原版）"。

**修改后原文**（test.md 新增「前置检查」节）：
```markdown
## 前置检查
- 对比 `<题目名>/src/` 各 .m 文件修改时间与 Phase 1 生成时间；发现人工修改的文件须在日志与报告中标注"人工修改文件：...（修改时间）"，并对其中的数学逻辑逐条复查映射。
```

#### 3.3-b 数据格式不符处理未定义（必须改）

**问题**：
CSV 列数不对、类型错误、缺失值等情况未定义行为。Claude 若静默补零/丢弃行，会**直接污染最终结果且无人知晓**——这是最危险的静默失败模式。

**建议**：
读取数据后立即做格式断言（行列数、类型、缺失值计数），不符时输出"期望格式 vs 实际格式"对比并**停止流转**，由编程手澄清；**禁止静默修复数据**（补零、丢行、插值均须显式征得同意）。

**修改后原文**（CLAUDE.md Phase 0 数据检查条）：
```markdown
- 数据格式校验：按映射表中的数据规格断言（行数/列数/类型/缺失值）。不符时在对话中给出"期望格式 vs 实际格式"对比并停止，禁止静默补零或丢弃行；由编程手澄清后再继续。
```

---

## 五、测试与判定科学性

### 4.1 稳定性阈值改为相对指标（必须改）

**问题**：
`std < 1e-3` 是绝对阈值：对量级大的问题（如目标函数值 1e6，绝对 std 1e-3 意味着 9 位有效数字，算法根本达不到）→ **过严误报**；对量级小的（如 1e-5，std 1e-3 意味着 100% 波动）→ **过松漏报**。一个固定绝对阈值无法覆盖不同量级的问题。

**建议**：
主指标改为**相对标准差** `std/|mean| < 1%`（1e-2），绝对 1e-3 仅作为 |mean|≈1 时的参考；随机算法单独分层（见 2.3-b）。与 2.3-b 合并落地。

### 4.2 收敛性判据缺失（必须改）

**问题**：
仅说"若为迭代算法，检查收敛性"，未定义任何收敛判据——该测试项当前**不可执行**，每次判定全靠临场感觉。

**建议**：
定义三级判据，满足其一即判收敛：
- ① 在最大迭代次数内满足算法自身终止条件（容差/梯度阈值）；
- ② 目标函数值序列最近 5 次迭代的相对变化 < 1e-6；
- ③ 收敛曲线（迭代次数 vs 目标函数值）保存至 `<题目名>/figures/`，报告中附文件位置。

**修改后原文**（test.md 测试项）：
```markdown
- **收敛性检验**（迭代算法）：满足以下任一即判收敛：
  ① 在最大迭代次数内满足算法自身终止条件（容差/梯度阈值）；
  ② 目标函数值序列最近 5 次迭代的相对变化 < 1e-6；
  ③ 收敛曲线（迭代次数 vs 目标函数值）保存至 `<题目名>/figures/`，报告附文件位置。
```

### 4.3 缺参考解对比（建议改）

**问题**：
现有测试项（维度、特殊值、稳定性、收敛性）均为**自洽性**检查——代码与映射同时出错但自洽时（如目标函数符号翻转、约束漏掉一条且数据恰好不触界）无法被发现。这是整个测试体系最危险的盲区。

**建议**：
增加"参考解对比"测试项：建模手提供**至少 1 组已知输入-输出对**（解析解或可信数值解）；解析解相对误差 < 1e-6、数值参考解 < 1% 为通过。无参考解时做解法交叉验证（不同步长/不同求解器结果一致性）。

**修改后原文**（test.md 测试项新增）：
```markdown
- **参考解对比**（复杂模型必选）：建模手提供至少 1 组已知输入-输出对（解析解或可信数值解）；解析解相对误差 < 1e-6、数值参考解 < 1% 为通过。无参考解时进行解法交叉验证（不同步长/不同求解器结果一致性）。
```

---

## 六、Claude Code 能力边界

> 本节基于当前 Linux 虚拟机**实测**：MATLAB R2026a（26.1.0.3312084, Update 4）位于 `/home/sck/matlab/bin/matlab` 且在 PATH；`matlab -batch` 无头模式实测可执行并捕获输出（约 20s 冷启动）；Octave 未安装；实测中出现警告"找不到 $documents/MATLAB 的个人文件夹"（HOME 环境变量未设，不影响运行，但 -batch 下 save 到默认路径可能失败，建议所有 save 显式指定绝对路径）。

### 5.1 自动执行 .m 脚本 —— 可行，但需备用路径（建议改）

**问题**：
规则要求"自动运行测试并保存结果"。实测结论：**Claude Code 可以通过 Bash 调用 `matlab -batch` 并捕获 stdout/stderr，本机已验证可行**。但存在三个依赖：① MATLAB 已安装且在 PATH（本机满足）；② 许可证无冲突；③ 单次运行不超时（冷启动约 20s，大计算可能超 Bash 超时上限）。此外首次执行 Bash 命令需用户批准权限。

**建议**：
保留自动执行为主路径，同时每次生成一键脚本 `<题目名>/run_test.sh`；自动执行失败（未安装/无许可证/超时）时，由编程手手动执行脚本并将输出粘贴回对话，Claude 据此继续判定。

**修改后原文**（test.md 新增「执行方式」节）：
```markdown
## 执行方式
- 主路径：Claude 通过 `matlab -batch "run('<题目名>/tests/test_<模型名>.m')"` 自动执行并捕获输出（超时上限 300s）。
- 备用路径：同时生成 `<题目名>/run_test.sh` 一键脚本；若自动执行失败（MATLAB 未安装/无许可证/超时），由编程手执行 `bash <题目名>/run_test.sh` 并将输出粘贴回对话，Claude 据此继续判定。
```

### 5.2 目录创建与文件写入 —— 能力确认，无需替代方案（可选优化）

**问题**：
审查确认：**Claude Code 原生支持 Bash（mkdir -p）与文件写入工具，Phase 0"自动创建题目子目录"完全可执行**——本会话第一/第二阶段已实际完成目录创建与 5+ 个文件写入，实测通过。无需"生成命令由编程手复制执行"的替代方案。

**建议**：
保留自动创建，仅做透明化：/prep 完成后在回复中回显所用 mkdir 命令与最终目录树，供编程手核对。

**修改后原文**（prep.md「输出产物」节新增）：
```markdown
- 创建完成后，在回复中回显所用的 mkdir 命令与最终目录树，供编程手核对。
```

### 5.3 上下文窗口限制 —— 分步生成策略（建议改）

**问题**：
/code 一次生成主程序 + 多个子函数（可能数百行含注释）+ 后续测试脚本，长对话下可能触及上下文上限；且函数接口设计错误时（维度约定不符），一次性生成意味着全部返工。

**建议**：
/code 分两步：**先生成主程序**（含完整函数接口骨架与公式注释），等待编程手确认接口；确认后**逐个生成子函数**；全部完成后仍自动进入 /test（不破坏自动流转，只是增加一次接口闸门）。单文件超 300 行强制拆分子函数。

**修改后原文**（code.md 新增「生成顺序」节）：
```markdown
## 生成顺序（防止上下文溢出）
1. 首先生成主程序 `main_<模型名>.m`（含完整函数接口与公式编号注释），等待编程手确认接口；
2. 接口确认后，逐个生成子函数 `func_*.m`；
3. 全部生成完成后自动进入 Phase 2（/test）。
单文件超过 300 行时拆分为多个子函数文件。
```

---

## 七、修改优先级建议

### 必须改（9 项）
| 编号 | 内容 | 理由 |
|------|------|------|
| 1.1 | 引入 `.active_problem.txt` 状态文件 | 跨会话目标丢失会写错目录，最高风险 |
| 1.3 | /test 幂等重跑 + failure_notes.md | 失败后流程断档，实际使用必然触发 |
| 2.1-b | 模拟数据生成规则必须由用户提供 | 防止 Claude 臆造数据污染全部验证 |
| 2.3-b | 稳定性阈值按算法类型分层 | 随机算法必误报，一刀切阈值不可用 |
| 2.3-c | 失败报错含预期 vs 实际对比 | 无对比数据则报错无意义 |
| 3.2-b | "根据题目描述"来源明确化 | 当前执行路径不存在，规则悬空 |
| 3.3-b | 数据格式校验 + 禁止静默修复 | 静默丢数据是最危险的失败模式 |
| 4.1 | 阈值改相对标准差 | 绝对阈值对不同量级问题必然误判 |
| 4.2 | 收敛性判据定义 | 无判据则测试项不可执行 |

### 建议改（12 项）
1.2 多题目参数化 · 2.1-a 解析步骤模板 · 2.2-a 致命错误清单扩充 · 2.2-b 路径健壮化 · 2.2-c 覆盖保护 · 2.3-a 特殊值清单 · 2.4-a 执行摘要 · 3.2-a 确认语义 · 3.3-a 人工修改检测 · 4.3 参考解对比 · 5.1 备用执行脚本 · 5.3 分步生成

### 可选优化（3 项）
2.1-c 映射表 JSON 副本 · 2.4-b 向量化量化指标 · 5.2 目录树回显

---

以上建议请逐条审阅。回复格式：`维度X.问题Y：采纳/不采纳/需讨论`，我会根据你的反馈对相应文件进行针对性修改。

---

## 八、裁决记录（2026-08-13 编程手反馈）

全部 21 项建议均已审阅完毕：

- **采纳（20 项）**：1.1、1.2、1.3、2.1-a、2.1-b、2.1-c、2.2-a、2.2-b、2.2-c、2.3-a、2.3-b、2.4-a、2.4-b、3.1、3.2-a、3.2-b、3.3-a、3.3-b、4.1、4.2、4.3、5.1、5.2、5.3
- **采纳并附修改意见（1 项）**：2.3-c —— 编程手自述对 MATLAB 不熟悉，请求介入时除"预期值 vs 实际值"对比外，Claude 还须依据失败类型给出 2–4 条初步排查建议（如 size() 维度检查、`*` 与 `.*` 区分、dbstop if error 定位等），并同步写入 failure_notes.md。
- 编程手补充说明：实际工作可能跨会话进行，但一般不会并行处理多题 → 单活跃题目的 `.active_problem.txt` 状态机制确认适用。

已按裁决完成全部修改：CLAUDE.md、.claude/commands/{prep,code,test,report}.md 均已落盘。

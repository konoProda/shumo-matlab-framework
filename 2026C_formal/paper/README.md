# 2026C 论文 LaTeX 源码（编程手侧）

本目录是 **论文的 LaTeX 源码**，自包含、可直接打包提交。
编译与渲染由编程手另做——本目录只保证**源码形式正确**，未在本机编译过（本机无 TeX 引擎）。

```
paper/
├── main.tex              导言区 + \input 各章节
├── latexmkrc             固定 XeLaTeX 引擎
├── README.md             本文件
├── sections/             逐章正文（12 个 .tex）
├── tables/               表格片段 9 个（题目强制表 8 + 附录 C 表 1）
├── figures/              10 张定稿 PNG（ASCII 名）
├── code/                 附录 B 的代码副本 4 个
├── values.json           【待补】填充所依据的全部数字（由 collect_values.py 生成）
├── build_tex.py          初稿 Markdown → sections/*.tex
├── gen_tables.py         生成题目强制表 1/2/3
├── collect_values.py     从 outputs/ 汇总数字 → values.json
├── fill_gaps.py          填 116 处【待补】
├── insert_mandated_tables.py   把强制表接入正文
├── insert_figures.py     复制图件并插图（含引导句与解释句）
├── prep_appendix_code.py 从 ../src/ 取附录 B 代码
├── check_static.py       静态自检（L0–L7，见下）
├── check_numbers.py      数字一致性核查表（确认点 #3 的输入）
├── check_report.txt      静态自检报告（生成物）
└── number_check.txt      数字核查表（生成物）
```

## 编译

```bash
cd 2026C_formal/paper
latexmk -xelatex main.tex        # 或 latexmk main.tex（latexmkrc 已指定 xelatex）
latexmk -C                       # 清理中间文件
```

**必须用 XeLaTeX**：`ctexart` 在 PDFLaTeX 下会在中文字体处直接报错。
若 `latexmk` 不可用，`xelatex main.tex` 连续跑两遍即可（第二遍解交叉引用）。

## 源码规模

| 项 | 数量 | 说明 |
|---|---|---|
| 编号公式 | **85** | `equation` 环境，全文连续编号 1–85 |
| 图 | **10** | 均为人工修证后的定稿 |
| 表 | **24** | 正文内联 15 + `tables/` 片段 9 |
| 章节文件 | 12 | `sections/*.tex` |
| 附录 B 代码 | 412 行 | 预算 426 行（6 页 × 71 行，`\scriptsize` 估） |

## 三条容易踩的编号约束（改源码前务必先读）

1. **公式编号必须全局连续**。`main.tex` 里把 `\theequation` 显式重定义为 `\arabic{equation}`，
   且**没有**用 `\numberwithin{equation}{section}`。
   一旦改成按节编号，85 个公式会变成 `5.1 … 8.31`，与初稿的 tag 1…85 全部错位。
2. **章节号必须显式重定义** `\thesection/\thesubsection/\thesubsubsection`。
   `titlesec` 的 `\titleformat` 只改**显示标签**，不改 `\thesection` 本身；
   只写 `\titleformat` 会让交叉引用拿到阿拉伯数字，出现"一、…"与"1.1"对不上的错号。
3. **图件用 ASCII 文件名**（`q1_day_plan.png` 等）。原文件名含中文与空格，
   部分 TeX Live 版本下 `\includegraphics` 会 `File not found`。
   原始中文名仍是交付名，见 `../figures/`。

## 首次编译后需人工确认的 7 项

以下问题**无法静态查出**，只有真正编译一次才会暴露：

1. `titlesec` × `ctexart` 的兼容性（`\chinese{section}` 作为 `\thesection` 有无警告）；
2. `\text{中文}` 在数学环境里的字体落位（正文中多处 `\text{…元}`）；
3. `\VerbatimInput` 内中文注释的字体（附录 B 的代码含中文注释与 `……`）；
4. 表格是否溢出：`symbol.tex` 的 6 张符号表列数较多，最宽的一张需目视确认；
5. 浮动体 `[H]` 是否产生孤页（全部表用 `[H]`，图用 `[htbp]`）；
6. 摘要是否**恰好占一页**（`abstract.tex` 末尾有 `\newpage`）；
7. 全文页数是否落在 25–30 页（规范 §二）。

## 已知限制与待补项（诚实清单）

| 项 | 状态 | 说明 |
|---|---|---|
| **参考文献** | ⚠️ **只有 1 条** | 初稿全文 0 处 `\cite`，仓库内也没有本队整理的文献清单。规范禁止虚构文献，故只保留了赛题本身这一条可核实的出处，其余留骨架待填。**需论文手提供真实文献清单。** |
| **个人信息** | 未设占位符 | 初稿没有作者/学校/队号区块（正文匿名，身份信息在竞赛的编号专用页）。规范 §八要求"留占位符"，但擅自造一个作者区块会与匿名要求冲突，故**保持原样未加**；若本队格式需要作者行，请指明放置位置。 |
| **建模流程图** | 待补 | 初稿摘要后留有流程图占位，该图属建模手交付物、不在 `figures/` 内。已在 `sections/analysis.tex` 末尾以注释登记，**未插入 `figure`**，避免编译时找不到图片。 |
| **附录 B 页数** | 估算 | 6 页为按行数推算（412 行 ÷ 71 行/页），未实测。若实际超出，可把 `main.tex` 的 `\fvset{fontsize=\scriptsize}` 调小一档。 |
| **附录 B 的代码范围** | 四个主程序 | 受 6 页上限约束，只收了四个入口程序；`func_build_*`、`func_roll_*` 等核心子函数随支撑材料提交。`main_q1.m` 末尾的纯打印段已整段略去并在文中标注。 |
| **正文数字口径** | 已统一 | 电价预测 MAE 采用**正式运行**口径 `0.0489 → 0.0432`；`outputs/测试记录/preprocess_log_q4.txt` 里的 `0.0480 / 0.0388 / 0.0341` 是**开工前探针**的旧值，不要引用。 |
| **K=8 稳定性** | 已完成 | 问题三、问题四的 K=8 对照均已跑完，数值已进正文表 10 与表 13。 |
| **摘要取整写法** | 提示，未改 | 问题三紧急购电量正文与表格写 `185 523.91 kWh`，摘要写 `185524 kWh`（取整）。同一指标两种精度，评审若对照会看到差别；摘要文字属论文手原文，按"文字少改"原则**未动**，如需统一请指明。 |
| **建模手来文一处笔误** | 以代码为准 | 《问题一_建模与实现总览》§6.4 写"基准 35126.85 元"，代码输出为 **35126.95**（`result1.xlsx` 与 `final_results_q1.mat` 一致）。该文自身的差额 −12925.10 只在 35126.95 下成立，可判定 35126.85 是笔误。**论文采用 35126.95。** |
| **问题一基准与灵敏度无独立产物** | 出处为文档 | 无储能基准（61 789.94 kWh / 48 052.05 元）与储能效率灵敏度（0.85→36603.41、0.95→33767.03 元）**没有留存 .mat/.log**，出处是建模手来文与 PH 备份档。已逐条验算自洽（见 `check_numbers.py` 的注释）。若需正式留档，建议补跑一次并落盘。 |

## 数字溯源

正文每个数字都可追到文件，逐值对照见附录 C 的表 `tab:c-index`。
生成链路：`outputs/` 结果文件与 `result*.xlsx`
→ `collect_values.py` → `values.json` → `fill_gaps.py` → `sections/q3.tex`、`sections/q4.tex`。

`collect_values.py` 里两处口径判断值得注意，改动前请先读注释：

- **问题二的汇总掩码**：`final_results_q2c_L2.mat` 对全部 365 天都算了结果，
  只有 `res.ok` 标记的 **334 天**属于报送窗口，统计必须先取掩码；
- **能量单位**：`em_m` / `curt_m` / `waste_m` / `chg_m` / `dis_m` 已是**每时段 kWh**，
  **不再乘 `dt`**。该结论由 Q3 汇总结构体的 `em_win`/`curt_win`/`waste_win` 三值比对得出（比值恰为 1.0000）。

## 静态自检

```bash
python3 check_static.py          # L0–L7 全层，逐项 PASS/FAIL → check_report.txt
python3 check_numbers.py         # 数字一致性核查表 → number_check.txt
```

**`check_static.py`**（14 项，当前 14/14 PASS）覆盖：编码（BOM/CRLF）、
配对（花括号 / `\[ \]` / 环境栈 / `\left\right`）、计数不变式（公式 85、表 24、图 10）、
文本模式危险字符（`# & _ ^` 与**裸 `%`**）、规范合规、Markdown 残留、
引用与浮动体、附录代码排版开关与页数预算。

> 该自检器做过**反向验证**：注入"手写标题序号 / 裸 `%` / 未闭合环境 / `\numberwithin` /
> `\input` 目标缺失"五类缺陷后，五类均被对应层抓到（14 项中 5 项 FAIL）。
> 未做反向验证前，它曾因 `strip_comment` 被当成"整篇"调用而在全文第一个 `%` 处截断，
> 导致 L1 只检查了前几行、裸 `%` 完全没查——那版是"永远 PASS 的假检测器"。

**`check_numbers.py`**（36 行核查项，当前 36/36 一致）把正文每个关键数字反查到
`outputs/` 的**字段级出处**，并做双向核对：正向查"出处值是否进了正文"，
反向查"正文里的大额数字是否都有出处"。反向清单当前为 **0**。

## 确认点 #3 的用法

编程/论文手做数字一致性核对时，直接看 `number_check.txt`：每行是
`论文处 | 论文数值 | 出处文件 · 字段 | 核查`，逐条打勾即可。
表里的 36 项已覆盖摘要与四问的全部关键结论数字。

## 表格编号与赛题"表 1/表 2/表 3"的关系

赛题要求"以表 1 的格式给出……"，这里的**表 1/表 2/表 3 是赛题中的表号**，
指的是**格式**，不是要求论文里的表就叫表 1。论文的表格编号由 LaTeX 全篇连续生成。
对应关系在每处引导句里写明（如"按题目表 1 与表 2 要求的格式，将上述结果整理如表 X 与表 Y"），
评审核对时不会混淆。

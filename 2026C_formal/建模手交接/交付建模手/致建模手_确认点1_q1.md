# 致建模手｜确认单 · 确认点 #1 · 问题一

> 题目：2026C 微网与外部电网电力调控策略 ｜ 当前问题：问题一
> 编制：编程手 ｜ 日期：2026-09-11
> **回复本单约需 3 分钟。只有第二节的 2 项需要你回复。**

---

## 〇、这份单子怎么用

编程手已完成问题一的数据预处理与建模解析。本单的唯一目的是：
**请你核对"我理解的模型"是否等于"你写的模型"**，并回复 2 个待定项。

- **第二节** —— 需要你回复（2 项）
- **第三节** —— 仅告知，无异议可不看（3 项）
- **第四节** —— 我实测到的数据事实（供你参考，无需回复）
- **第五、六节** —— 回复方式与后续影响

---

## 一、我收到了什么 / 产出了什么

**收到**：`建模手交接/接收自建模手/问题一_建模交接与算法实现(2).md`

**产出**（编程侧内部文档，供追溯用，**你无需阅读**）：

| 文件 | 用途 |
|---|---|
| `outputs/math_to_code_mapping_q1.md` | 数学符号 → 代码变量 → 维度 映射 |
| `outputs/solve_strategy_q1.md` | 求解规模与风险预判 |
| `outputs/decisions_q1.md` | A/B/N/R 裁决清单（编程手核对用） |
| `outputs/preprocess_log_q1.txt` | 数据格式校验日志 |

---

## 二、需要你回复的（2 项）

### C1 【需你修正文档】建模文档 §4.1 的时间戳表述与附件实际不符

**现状**：§4.1 的表格写"题目数据中的时间戳表示对应 10 分钟时段的**起始**时刻"，并列出：

| 数据时间戳 | 实际调度时段 |
|---|---|
| **0:00** | 0:00–0:10 |
| … | … |
| 23:50 | 23:50–24:00 |

**实测**：附件1 的 144 个时间标签是 **`00:10, 00:20, …, 23:50, 0:00+1`**，
**不存在 `0:00` 这一格**。附件2 / 附件4 的列标签同样如此。

**关键点**：**你的结论是对的，只是证据表写错了。**

- 你写的 t=1 ↔ `[0:00, 0:10)`、t=144 ↔ `[23:50, 24:00)`、时间轴 `[0:00, 24:00)` —— **完全正确，我按此实现**。
- 但这个结论只有在把标签读作时段**结束**时刻时才成立：
  - 读作**结束**：标签 `00:10` = 时段 `[00:00, 00:10)` → 正好是 t=1 ✅
  - 读作**起始**：标签 `00:10` = 时段 `[00:10, 00:20)` → 与 t=1 冲突，且会推出时间轴 `[00:10, 24:10)`，与附录1 的 0:00 / 24:00 储电量边界矛盾 ❌

**请你做的事**（二选一，30 秒）：

- ☐ **A. 修正 §4.1**：把表格里的 `0:00 … 23:50` 改为 `00:10 … 0:00+1`，
  并把"**起始**时刻"改为"**结束**时刻"。改完无需重发全文，回复一句即可。
- ☐ **B. 不改文档**，仅在此确认："§4.1 结论正确，标签按结束时刻理解"。

> 代码不受影响（我已按你的结论锁死）。此项纯粹是**让文档与代码口径一致**，避免后续问题反复。

---

### C2 【需你回复】/test 阶段缺少"参考解"

**情况**：建模文档 §16 给了三类验证（能量平衡残差、约束检查、经济性规律），
但没有给出"已知输入 → 已知输出"的参考解。按流程，/test 阶段需要这一项。

**编程侧已自行构造了两条验证路径**，请你确认是否可用：

| 路径 | 做法 | 预期 |
|---|---|---|
| **R1 手算算例** | 缩样：T=2，PV=0，L=600 kW，π=[0.4, 0.5] 元/kWh，η=0.9，P_max=5000 kW | 最优购电费 **89.38 元**（不套利为 90.00 元；套利因 0.4/0.81=0.4938 < 0.5 而更优） |
| **R2 LP 松弛下界** | 去掉二元变量与互斥约束求 LP 松弛最优值 \(Z_{LP}\) | 必有 \(Z_{LP}\le Z_{MILP}\)；若两者相等（差 < 0.01 元），即同时证明 MILP 已达全局最优、且无时段同时充放电 |

**请你回复**（二选一）：

- ☐ **A. 同意采用 R1 + R2**（推荐，编程侧可直接落地，不需要你额外提供数据）
- ☐ **B. 我另行提供参考解**：请附上输入与对应输出（可手算的小规模即可）

---

## 三、仅告知，无需回复（3 项）

### C3 结果模板的时间标签整体错位一格

`附件5/result1.xlsx` 的 144 行标签是 `0:10-0:20, …, 0:00+1-0:10+1`，
首格写的是 `0:10-0:20`，而不是 `0:00-0:10`（两个模板的末格写法还互相矛盾）。

由于数据与模板都是 **144 格**，"按序号一一对应"是唯一可行解——否则会丢掉首槽、又多出一格无数据可填。
**我已按序号对应填充**。附带说明：论文表 1 要求填的 `10:00-10:10`，对应模型第 61 槽，
它在模板里的字面标签是 `10:10-10:20`。**论文表格按物理时段报告即可**，不影响结果。

### C4 充放电效率 90% 的口径

题面只写"充放电效率为 90%"，未说明是单程还是往返。
**我按你的建模文档取两侧各 0.90**（往返 81%）。
若你本意是往返 90%（则两侧各 ≈0.9487），请告知——会影响数值结果。
若维持现状，建议论文"模型假设"处写明取法。

### C5 弃光的经济含义

题面未给上网电价，故按"**不允许售电**"处理，弃光量为零成本。
建议论文"模型假设"处说明这一口径选择。

---

## 四、我实测到的数据事实（供你参考，无需回复）

| 编号 | 事实 |
|---|---|
| F1 | **附件1 是"典型日"**：其三列分别等于附件4、附件2 对应列的**全年逐槽平均值**（逐元素比对，相关系数 **1.00000**，差值 ≈0）。故问题一代表年平均水平，不代表某个具体日期 |
| F2 | 附件1 电价**日内非恒定**：137 个不同取值，最高 20:30-20:40 = 1.3952，最低 05:30-05:40 = 0.3713 元/kWh，峰谷价差 1.0239 |
| F3 | 附件1 光伏 04:30–19:20 出力，89/144 槽非零，峰值 7612.32 kW（12:00-12:10）；**光伏 > 负载 的时段 30/144 槽** |
| F4 | 附件1 时间列**存储类型混合**（前 60 个为时间值，后 84 个为文本），MATLAB 直接读表会出错 → 已改为代码程序化生成时间轴 |
| F5 | 数据健康：**无缺失、无非数值文本、无负值、无重复**，无需任何插补或修正（含 2025C 复盘教训的文本污染专项扫描） |

> F1 与 F2 建议写入论文的数据说明部分——它们能直接支撑"问题一为何是单日优化"这一建模选择。

---

## 五、回复方式

任选其一：

1. **直接在本文件第六节填写**，存回 `2026C_formal/` 或 `2026C_formal/outputs/`，告知编程手；
2. 或直接在对话/群内以文字回复（列出 C1、C2 两项即可）。

---

## 六、回复区（请在此填写）

```
C1  §4.1 时间戳表述
    ☐ A. 我方修正建模文档（表格改为 00:10…0:00+1，并改为"结束"时刻）
    ☐ B. 不改文档，确认：§4.1 结论正确，标签按结束时刻理解
    备注：__________________________________________

C2  /test 参考解
    ☐ A. 同意采用 R1（手算算例 89.38 元）+ R2（LP 松弛下界）
    ☐ B. 我方另行提供参考解（附件/说明：______________）
    备注：__________________________________________

C4  效率口径
    ☐ 维持两侧各 0.90     ☐ 改为往返 0.90（两侧各 0.9487）
    备注：__________________________________________

其他补充意见：
__________________________________________________
__________________________________________________
```

---

## 七、你回复之后会发生什么

| 你的回复 | 后续动作 |
|---|---|
| **C1 选 A**（你修正建模文档） | 编程手重跑 `/prep`，更新本确认单与 `decisions_q1.md` 后**再次请你确认**；若仅为文字修正、不影响模型，则直接放行 |
| **C1 选 B** | 编程手在 `decisions_q1.md` 中记录口径，**直接放行** |
| **C2 选 A** | 参考解写入 /test 脚本，直接放行 |
| **C2 选 B** | 编程手等待你的参考解文件，收到后放行 |
| **C4 有改动** | 编程手更新映射表与裁决清单，**再次请你确认** |
| **全部无异议** | 编程手进入 `/code`，开始生成问题一的 MATLAB 代码 |

---

*本确认单由编程手编制。若对任何条目的表述有疑问，欢迎直接指出——单子写得不清楚是编程手的问题，不是你的。*

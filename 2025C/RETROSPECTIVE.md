# 2025C 复盘（RETROSPECTIVE）

> 组内文档，不交付。日期：2026-08-25。

## 做得好的

1. /prep 阶段两轮建模检查（A1~B10、N1~N10）全部闭环，杜绝了分段缝隙、共线、指标方向、量纲等 6 处公式层面缺陷进入代码。
2. 小规模探针（K=2 微型数据）先于全量跑通装配，问题暴露在秒级探针上而非全量运行上。
3. /test 的"可手算预期"特殊值用例（切点搜索手算用例误差<1e-9）有效抓住了行/列方向、η 覆盖等实现错误。
4. 求解策略预判准确：切点枚举剪枝后全量仅 ~5 分钟，MC 固定参数口径避免了数十小时的不可行方案。

## 踩坑记录（供后续题目复用）

| # | 坑 | 现象 | 处置 |
|---|---|---|---|
| 1 | `preprocess` 与 Computer Vision Toolbox 内置函数重名 | 报"需要 Computer Vision Toolbox" | 改名 `preprocess_data`；新题命名前先 `which` 查重 |
| 2 | readtable 类型推断丢值：数值列混存文本数字（本次为 `'≥3'` 截断编码） | 静默 NaN → 回归丢 27.7% 记录、孕妇层 NaN 传播 | 改用 readcell 逐列统一转换；/prep 数据校验要查"非数值文本"而非只看空值 |
| 3 | `unique([20,10])` 排序打乱"先 20 后 10"的回退顺序 | n_min 约束被静默放宽 | `unique(...,'stable')`；回退顺序类逻辑写测试断言 |
| 4 | 行向量 cost × 行向量 pg 隐式扩展成矩阵 | 赋值维度错误 | 图/表方向统一（cost 返回行向量）并在 8 项自检⑤中逐处核对 |
| 5 | 灵敏度矩阵与主解标志同名（eta_ok）互相覆盖 | 逻辑索引越界 | 主解字段与灵敏度字段命名分离（eta_ok vs eta_ok_mat） |
| 6 | 脚本裸 `end`（script+local function 结构） | 语法错误 | 脚本体内不允许 end；写测试/探针脚本时注意 |
| 7 | 会话重启导致后台任务被终止、cwd 重置 | 误判"卡死" | 长任务用绝对路径；中断先查输出文件与进程再下结论 |
| 8 | 背景命令管道 tail 包装进程滞留 | 误判 MATLAB 未退出 | 后台 MATLAB 任务不接管道，直接重定向文件 |

## 工作流建议（给编程手）

- /prep 数据校验清单增加一项："数值列中非数值文本扫描"（本次 `'≥3'` 直到 /test 才暴露，虽未造成返工但应更早发现）。
- 图件清单前置对齐有效避免了出图返工；重复图（q3_eta_sens vs sens_eta）在清单阶段被发现并删除。
- 本环境无 GPU 渲染（图形加速警告），图件走软渲染正常；中文 Noto CJK 字体齐全，中文图内文字可用。

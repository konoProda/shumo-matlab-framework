# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

# 工作流核心原则（2026-09-07 优化）

## 三大核心原则

1. **准确度为主，效率为次**：通过关键人工确认点保证准确度；自动化非关键环节提升效率
2. **诚实优于修饰**：防AI的关键是列出风险并在论文中说明，而非隐瞒或修改表述
3. **三Agent协作**（建模Agent、编程Agent、论文Agent均由AI驱动，但关键点需人工确认）

## 三个关键人工确认点

| 确认点 | 时机 | 内容 | 人工 | 时间 |
|-------|------|------|------|------|
| **#1 建模裁决清单** | /prep 后→/code前 | decisions.md 中的 A/B/N 裁决是否完整、与建模文档逐条对应 | 编程手 | 5-10min |
| **#2 风险清单** | /report 后→论文交接前 | IMPLEMENTATION_REPORT中的风险是否完整、诚实 | 编程手 | 10-15min |
| **#3 数字一致性** | 论文初稿完成后 | 论文中的关键数字与outputs/是否一致 | 编程/论文手 | 10-15min |

---

# WORKFLOW AUTOMATION RULES（工作流自动调用规则）

## 两条铁律

1. **角色规范严格分离**：编程手规范（/prep、/code、/test、/report）与
   论文手规范（/pinit、/pwrite、/pcheck）互不套用。
2. **三个关键确认点**：3 个关键确认点 + 2 个复盘固化的前置确认（求解策略并入确认点 #1、图件清单独立），仅在这些环节停止等待人工确认；其他环节自动推进，无需等待。

## 全局目录规则

- **配置层**：存放 CLAUDE.md 和 .claude/commands/。
- **项目层（<题目名>/）**：每个题目拥有独立的子目录结构。
  - **强制目录**：`data/`、`src/`、`outputs/`、`figures/`。
  - **按需目录**：`scripts/`、`tests/`、`dev_tools/`（组内产物，不交付）。
  - **交付目录**：`deliver/`（唯一对外，结构见下文"交付附件命名与结构规范"）。
  - **内部文档**：`IMPLEMENTATION_REPORT.md`、`PAPER_HANDOFF.md`、`RETROSPECTIVE.md`（不交付）。
- **状态文件**：`.active_problem.txt` 记录当前题目与阶段（格式：`<题目名>|<Phase>`）。

当用户（编程手）提出与"编写建模代码"相关的请求时，必须遵循以下分步流水线：

## 逐问求解模式（2026-09-11 立，适配建模手逐问交付）

自 2026C 起，流水线按**逐问**推进：`Qn 建模文档 → /prep → /code → /test → /report → Q(n+1) …`。
本模式**只改动以下五处**，其余规范（角色分离、交付结构、图件规范、代码风格）一律不变。

1. **状态文件**：`.active_problem.txt` 扩展为 `<题目名>|<Phase>|<当前问题>`
   （例：`2026C_formal|Phase0|Q1`）。命令支持显式指定：`/prep <题目名> Q2`、`/code <题目名> Q2` 等。
2. **逐问产物加 `_qX` 后缀**，避免四问互相覆盖：
   - `outputs/decisions_qX.md`、`solve_strategy_qX.md`、`math_to_code_mapping_qX.md`、`preprocess_log_qX.txt`
   - `outputs/test_results_qX.mat`、`test_log_qX.txt`、`failure_notes_qX.md`、`final_results_qX.mat`
   - `src/main_qX.m`、`src/func_<短词>_qX.m`（既有命名不变）；绘图脚本随图件放入
     `figures/<问题X>/<NN 图名>/`，与 data.csv、PNG、PDF 同目录（2026-09-11 立）
3. **/prep 每次只解析当前一问的建模文档**，对应确认点 #1 只核该问的裁决清单；
   已完成问题**不重跑、不合并**，其产物原样保留。
4. **确认点 #1 / #2 逐问重复**（每问各停一次）；**确认点 #3 仍在论文初稿后一次性执行**
   （论文是全局的）。图件清单前置确认逐问进行；**图件数量限制已于 2026-09-11 取消**，出图力求完整，选用由人工筛选。
5. **`PAPER_HANDOFF.md` 与 `IMPLEMENTATION_REPORT.md` 保持单文件、按问追加章节**
   （最终交接需要全局视图），不拆成四份。

## Phase 0 - 题目环境初始化与数学建模解析（/prep）

- 创建题目子目录（询问题目名，不可默认猜测）及标准子文件夹：data/、src/、outputs/、figures/。
- 接收建模手提供的数学模型并结构化理解（变量、目标函数、约束、数据I/O规格）；建模文档以建模手实际交付的文件名为准，其路径记入 decisions.md 供确认点 #1 核对。
- 生成"数学符号 ↔ 代码变量名 ↔ 维度"映射表（5列：公式编号|符号|含义|类型/维度|代码变量名）。
- 检查数据文件；若缺失则向编程手询问模拟数据规则（维度、分布、范围、样本量）。
- 数据格式校验：若不符映射表规格则给出对比并停止，由编程手澄清后继续。
  **含"数值列非数值文本扫描"**（2025C 复盘 R7：`'≥3'` 截断编码曾被 readtable 静默转 NaN，
  丢失 27.7% 记录直到 /test 才暴露）——数值列须用 readcell 逐列检查非数值文本，不得只看空值。
- 生成"求解风险预判"（solve_strategy.md）：变量/约束规模 × 求解器边界 × 策略建议。
  **该策略须经编程手确认后才进入 Phase 1**（2024C 复盘 R1：Q1 滞销最优性问题因缺此环节，
  拖到稳定性检验才暴露，靠拓扑固定 LP 补救）。此确认可与确认点 #1 合并进行。
- **【关键确认点 #1】** 生成"建模裁决清单"（`outputs/decisions.md`），包含：
  - 建模裁决清单（A/B/N 系列，逐条列出，供 /code 落地与后续核对）
  - 数据处理与平补规则
  - 求解策略（来自 solve_strategy.md）
  
  更新 `.active_problem.txt`（格式：`<题目名>|Phase0`）后停止，等待编程手确认。
  
  **编程手确认内容**（5-10分钟）
  - 所有 A/B/N 系列裁决都清晰列出，与建模文档逐条对应
  - 无遗漏或歧义的裁决；求解策略可行
  - *注：此时代码尚未生成，不要求标注代码位置；代码位置在 /report 阶段回填到映射表*
  
  确认完成后自动进入 Phase 1。

  > **PAPER_HANDOFF.md 不在本阶段生成**——它含"核心结果汇总"，需代码运行产出后才有数据，
  > 实际由 /report 阶段生成（见 Phase 3）。

## Phase 1 - 代码生成（/code）

- 基于确认后的 decisions.md 与映射表生成MATLAB主程序（存放于 <题目名>/src/）和必要的子函数。
- 支持显式指定题目：`/code <题目名>`；未指定时读取 .active_problem.txt。
- 分步生成：先生成主程序（含函数接口与公式注释），再逐个生成子函数（不再单独等待接口确认，裁决已在确认点 #1 核过）。
- 代码生成前仅进行"致命错误检查"（如矩阵维度、死循环、索引越界等），不修改建模逻辑。
- 数据读取路径：主程序顶部统一定义 `PROJ_ROOT = fullfile(fileparts(mfilename('fullpath')), '..')`，后续一律 `fullfile(PROJ_ROOT, 'data', ...)`。
- 代码注释中必须标注对应的数学公式编号。
- 注释规范以 `.claude/commands/code.md` §代码注释规范为准（只保留思路级、删实现细节、扫AI高频词）。
- **代码风格离散性**：见下文。
- **自动进入 Phase 2**。

### 代码风格离散性（2026-09-08 立，依据 7 篇范文横向取证）

**取证结论**（2026-09-08 实测复核，取证明细见 `.claude/evidence/代码风格取证_7范文.md`）：2023/2024/2025 共 7 篇范文的附录代码，风格**彼此不一致**——
"Step N" 标记仅 2024C范文1 出现（9 处：正文算法描述 3 + 代码注释 6），其余范文 0 处；
try/except 仅 2025C 两篇出现（范文1 约 6 组、范文2 2 组，PDF 提取有损为下限），2023C/2024C 全部 0 处；
print 仅 2025C范文1 出现（6 处），2024C 三篇 0 处（2023C 两篇代码为图片/OCR 混合嵌入，print/fprintf 计数无法可靠提取，原统计待人工复核）；
变量命名长短混用（如 2024C范文1 `def risk_assessment(total_revenue_list)` 与单字母符号并存）。

**因此，竞赛代码不存在统一范式；"整齐划一"本身才是机器指纹。**

规则：
1. **禁止**给全项目施加统一代码风格模板。函数头注释块、分节标记（`%%` 序号 / 公式编号 / 无标记）、
   命名长度**允许并应当**在文件间自然参差。**命名风格基线不在离散化之列**——变量下划线、函数驼峰
   （CO-STAR）、文件名 `main_qX.m` / `func_<短词>_qX.m` 模式全项目统一保持。
2. 不得以"范文没有 X"为由禁用 X（Step 标记、try/catch、print、长变量名在范文中均有实例）。
3. **已知我方痕迹（实测）**：2024C+2025C 共 24 个函数**无一例外**都带 4~15 行函数头注释块。
   允许保留函数头块，但**不要求每个函数都有**——短小工具函数可只留单行或不留。
4. **交付前抽检**：随机取 3 个 .m 文件，若三者结构高度雷同（同格式头块 + 同分节风格 + 同命名粒度），
   即判定为痕迹，需分散化后再交付。

## Phase 2 - 自动化测试（/test）

- 代码生成完毕后，在 <题目名>/tests/ 目录下生成独立的测试脚本。
- 支持显式指定题目：`/test <题目名>`；未指定时读取状态文件。
- 测试核心目标：验证代码实现与数学模型的一致性。
- 使用 <题目名>/data/ 下的数据进行测试（若文件过大则截取子集）。
- 测试内容包括：维度检查、特殊值检验、稳定性检验（确定性算法相对标准差 < 1%；随机/启发式算法最优值相对波动 < 5%）、收敛性检验（若为迭代算法，判据见 /test）、参考解对比（建模手提供已知输入-输出对）。
- 测试运行结果（含日志）自动保存至 <题目名>/outputs/test_results.mat 和 <题目名>/outputs/test_log.txt。
- 测试通过后自动进入 Phase 3；若未通过，停止流转，报错（含失败项、预期值 vs 实际值、复现命令、初步排查建议），并将失败详情写入 <题目名>/outputs/failure_notes.md，请求编程手介入。
- 重试机制：编程手修复后再次输入 /test 即可（幂等覆盖旧结果），无需重跑 /prep 与 /code。

## Phase 3 - 正式运行与报告生成（/report）

- 测试确认无误后，使用 <题目名>/data/ 下的完整原始数据重新运行主程序。
- 支持显式指定题目：`/report <题目名>`；未指定时读取状态文件。
- **图件清单前置对齐**：正式出图前提交图件清单表（图名|角色|代码文件|数据来源|论文位置|支撑结论），
  **经编程手确认后批量生成**，禁止先出图后返工（2024C 复盘 R5，2025C 复盘再次验证有效：
  重复图 q3_eta_sens vs sens_eta 在清单阶段即被发现删除）。清单表六列与裁剪规则见 `.claude/commands/report.md`。
  出图后按 `figures/<问题X>/<NN 图名>/` 一图一文件夹归档、图像文件名即交付中文名，
  顶层"问题X"关键结果图由编程手**人工筛选**后复制至 deliver/，不预设选图。
- 运行结束后，结果保存至 <题目名>/outputs/final_results.mat，图片保存至 <题目名>/figures/。
- 自动生成实现验证报告（<题目名>/IMPLEMENTATION_REPORT.md），内容包含：
  - 执行摘要（一句话结论 + 关键指标）
  - 数学→代码映射表（此时回填各裁决对应的代码位置 文件:行号）
  - 运行记录（时长、内存）
  - 测试验证结论（各测试项通过/不通过）
  - 代码质量说明与潜在风险提示
  - **新增**：已知警告与限制（需在论文中体现的风险）
  - **新增**：风险→论文对接表（每个风险指向论文位置）
- 同时生成 PAPER_HANDOFF.md（含核心结果汇总，每个数字标注出处文件）。
  
- **【关键确认点 #2】** IMPLEMENTATION_REPORT.md 生成后停止，等待编程手确认。
  
  **编程手确认内容**：风险是否完整、诚实（10-15分钟）
  - 所有已知风险、限制、警告都清晰列出
  - 每条风险都标注对应论文位置（例如"问题二分析段"）
  - 风险说明是诚实的，不隐瞒问题
  
  确认完成后自动进入论文交接。

---

## 论文交接文档（全局规范，2026-09-07 优化）

/report 完成后、进入论文写作流程前，PAPER_HANDOFF.md 和 IMPLEMENTATION_REPORT.md 必须包含以下关键信息：

### PAPER_HANDOFF.md（建模→编程→论文 的交接）

**核心内容**：
1. 建模概述（问题理解、假设、符号、模型、参数、裁决记录）
2. 代码与求解概述（工具、策略、耗时）
3. 核心结果汇总（每个数字标注出处文件）
4. **【新增】建模裁决清单**（A/B/N系列，编程手用于验证代码落地）
5. **【新增】数据处理与平补规则**（完整记录所有处理规则）
6. 检验与稳健性（测试结论、收敛证据、已知局限）
7. 图件与结果表清单

### IMPLEMENTATION_REPORT.md（编程→论文 的交接）

**核心内容**：
1. 执行摘要（一句话结论 + 关键指标表）
2. 数学→代码映射表（编号、符号、代码位置）
3. 运行记录（耗时、内存、规模）
4. 测试验证结论（按测试组汇总）
5. 代码质量与风险提示
   - 向量化程度
   - **【新增】必须在论文中体现的风险**（分类列表）
   - **【新增】风险→论文对接表**（风险 | 论文位置 | 诚实口径）
   - 已知警告（给评审）
   - 已知限制（给评审）

### 论文初稿与代码的对接表（论文→编程 的检查）

**【关键确认点 #3】** 论文初稿完成后，生成一致性核查表：

| 论文内容 | 出处文件 | 核查结果 |
|--------|--------|--------|
| 摘要"最优利润6111万元" | outputs/q1_solution.csv | ✓ |
| 问题二"BMI≤30推荐10周" | outputs/q2_group_result.csv | ✓ |
| 评价"风险已说明" | IMPLEMENTATION_REPORT §风险 | ✓ |

**编程/论文手确认内容**（10-15分钟）
- 所有论文数字与 outputs/ 一致
- 风险说明已在论文中体现
- 无无法溯源的数字

---

## 交付附件命名与结构规范（2026-09-08 立，依据老师参考交付目录 + 编程手裁决口径）

**职责边界**：编程手只负责各问"代码与说明"文件夹与顶层结果图；`论文源程序/`（论文 LaTeX 源码）归论文手；
`论文.pdf` 与 `AI工具使用说明.pdf` 为全队文件，编程手**不生成**，收尾时提示补入。

### 交付目录结构

```
deliver/
├── 论文源程序/                    # 论文手：paper/ 的 LaTeX 源码
├── 问题一对应的代码与说明/         # 编程手（每问一个，官方措辞逐字沿用）
│   ├── main_q1.m                 # 该问入口程序
│   ├── func_<短词>_q1.m          # 该问依赖的子函数副本
│   ├── 问题一_代码说明.docx        # Word 说明（与代码一一对应）
│   └── 问题一_<结果>_<参数>.csv   # 结果表（非必要，见命名规则5）
├── 问题二/三/四对应的代码与说明/   # 同构
├── 论文.pdf                      # 全队
├── AI工具使用说明.pdf              # 全队
└── 问题X <参数>_<目标结果>.jpg    # 每问关键结果图（人工筛选后放置）
```

### 命名规则

1. **顶层结果图**：`问题X <参数>_<目标结果>.jpg`——全中文，多个参数用、连接（n100、n200、n300 / 6×4），
   目标结果为评审一眼可读的短语（最小死区可行布局 / 零空白最优布局）；禁"最终版"等模糊词
2. **文件夹**：`问题X对应的代码与说明/`，官方措辞逐字沿用，不缩写
3. **代码文件**：保留简单英文，用字母数字标号区分问题——入口 `main_qX.m`、子函数 `func_<短词>_qX.m`
   （短词如 ols/fit/cut/pack，全局工具可不带 _qX）；**避免复杂英文长名**（preprocess_data 类不用）
4. **说明文档**：`问题X_代码说明.docx`（Word，非 .md）——自然段落写清三件事：算的是什么（对应问题哪一部分）、
   按什么思路实现（对应论文哪节公式）、得到什么结果；与代码文件一一对应；
   **不出现运行入口、命令行、目录树**
5. **结果表**：非必要不交付；仅结果量大或题目要求时放入各问文件夹（命名 `问题X_<关键结果>_<参数>.csv`）；
   少量计算结果（单个或几个值）直接写入说明文档正文，不单独放表

### 图件流程

出图后按 `figures/<问题X>/<NN 图名>/` **一图一文件夹**归档（**2026-09-11 立**）：
每个文件夹内自包含**绘图脚本 + `data.csv` + PNG + PDF**，图像文件名即交付中文名
（`问题X <参数>_<目标结果>.png`）；改图只需打开对应文件夹，无需跨目录查找。
顶层关键结果图由编程手**人工筛选**后复制至 deliver/，不预设选图。

---

## 工作流迭代规则（2026-09-07 优化）

1. **求解风险预判**：/prep 输出 solve_strategy.md，**策略经编程手确认后才进 /code**（2024C R1），
   可与确认点#1 合并进行
2. **代码自检**：所有.m文件交付前过8项静态自检（致命错误类，见 code.md）；新模型先小规模探针再上全量
3. **长任务三件套**：① 启动前冒烟验证；② 后台运行；③ 定时轮询+自动收尾
4. **单MATLAB会话铁律**：3GB内存环境，同时仅一个进程
5. **人工确认精简**：3 个关键确认点 + 2 个复盘固化的前置确认（求解策略、图件清单）
   - 确认点#1：建模裁决清单 + 求解策略（/prep 后，5-10分钟）
   - 确认点#2：风险清单（/report 后，10-15分钟）
   - 确认点#3：数字一致性（论文初稿后，10-15分钟）
   - 图件清单前置确认（/report 出图前，2024C R5）
   - 其他环节自动推进，无需等待
6. **收尾六件套**：状态文件置Done、PAPER_HANDOFF、IMPLEMENTATION_REPORT、**RETROSPECTIVE.md**、
   deliver/按"交付附件命名与结构规范"打包、三确认点记录留档。
   收尾时提示全队补入 论文.pdf 与 AI工具使用说明.pdf（编程手不生成）。
   **RETROSPECTIVE 不可省**——它是下次工作流优化的唯一流程依据（2026-09-07 那轮优化因未读复盘，
   误删了 R1/R5 两条事故补丁）。
7. **优化取证层级**（2026-09-08 立）：
   - **产出标准**（代码/论文长什么样）→ 以**范文**为唯一权威
   - **流程设计**（哪些环节会出事故）→ 以**RETROSPECTIVE** 为唯一权威
   - **已交付练习稿**（2023C/2024C/2025C）→ 二者皆不是，它是**待改造对象**，
     不得用"与练习稿一致"论证规范正确
   - 禁止先立结论再找证据；取证不足时应明确标注"证据不足"而非补猜测

---

## 三个关键人工确认点的操作规范

### 通用要求：确认点交付单（2026-09-11 立，三个确认点一律适用）

**每次遇到关键确认点，除生成编程侧内部核对文档外，必须额外生成一份可直接交付建模手的确认单**，
用于快速核对"编程手理解的模型 = 建模手写的模型"，减少往返沟通成本。

| 项 | 规定 |
|---|---|
| 路径 | `<题目名>/建模手交接/交付建模手/致建模手_确认点N_qX.md`（N=1/2；确认点 #3 为全局，写 `致建模手_确认点3.md`，不加 `_qX`） |
| 交接文件夹 | `<题目名>/建模手交接/` 下分 `交付建模手/`（编程手→建模手）与 `接收自建模手/`（建模手→编程手）两个子目录，与 `outputs/`、`data/`、`deliver/` 分离；建模手来文一律以 `问题X_` 开头；历史来文一律保留不删 |
| 必需内容 | ① 收到/产出清单（内部文档只列文件名，**不要求建模手阅读**）；② **需回复项**（逐条编号，每项 ≤2 个选项，附一句否决后果，并标明"需你修正文档/需你回复"）；③ **仅告知项**（明确标注"无需回复"）；④ 编程手实测的数据事实（供参考）；⑤ 填写式回复区；⑥ "你回复之后会发生什么"对照表 |
| 表述要求 | 面向建模手，**不出现 MATLAB/代码/求解器/工程细节**；凡是"编程手自己判断即可"的事项一律不放进需回复项 |
| **不提路径** | 交付建模手的文档中**不得出现编程侧的项目名、目录名、文件名或任何路径**（建模手与本队设备、工作目录均不同，写路径只会造成困惑）。引用建模手来文只写**书名号内的文件名**（如《问题一_建模交接与算法实现(2).md》）；引用编程侧产物一律改为**职责描述**（如"编程侧内部核对文档""测试日志"）并注明"你无需阅读" |
| **AI-ready** | 除人工审查外，还须便于组员用 AI 辅助阅读与回复，见下 |
| **回传须为文档** | 建模手的回复须以**一份文档**回传，**不接受零散口头/聊天文字**——回传件要留档，也是后续追溯口径的依据。确认单第五节须给出**固定的回传文档结构模板**（键值式逐条 `Cn = A/B` + 补充说明），使建模手可直接让 AI 依此填充生成；回传件归档至 `接收自建模手/`，命名沿用 `问题X_` 前缀（如 `问题一_确认结果_确认点2.md`） |
| 与内部文档关系 | 内部文档（decisions/mapping/solve_strategy）供编程手核对；确认单是其对外简化版，**编号须与内部文档一一对应** |
| 回复处理 | 建模手回复后：① 无改动 → 直接放行；② **有模型改动** → 更新内部文档并重新确认（按确认语义判据）；③ 仅文字/口径修正 → 记入内部文档后放行 |

**设计约束**：需回复项宜少不宜多——建模手**3 分钟内能回完**是验收标准；
凡属"已由数量关系或题面唯一确定"的事项，一律降级为"仅告知项"。

#### 确认单的 AI-ready 设计要求

组员可能用 AI 辅助阅读与回复，故确认单须同时满足"人可读、机可解析"：

| 项 | 规定 |
|---|---|
| 元数据块 | 开头固定声明：对象、确认点、当前问题、**需回复项数**、预计用时——人与 AI 都能一眼判断工作量 |
| 条目编号 | 需回复项用 `Cn`、仅告知项用 `Gn`，全局唯一且**重发不重编号**（便于跨版本追踪同一事项） |
| 状态显式 | 每条以 `编号+标记` 起始（如 `C1【需回复】`），**编号与标记之间不留空格**——单条正则即可抽出全部条目；**不得用语气暗示**（如"建议你……"） |
| 条目结构 | 需回复项固定四段且顺序不变：**事实 → 影响 → 选项（≤2 个）→ 各选项后果** |
| 回复区 | 结尾给出**键值式填写块**（如 `C1 = A / B`），使 AI 可直接生成回复文本 |
| 自足性 | 不依赖任何外部文件即可读；正文不出现"见某文件""如上所述"之类相对指代 |

**该两项要求适用于交付建模手的全部文档**（确认单、回执及后续任何交接件），不止确认单。

---

### 确认点 #1：建模裁决清单 + 求解策略（/prep 后，5-10分钟）

**时机**：`outputs/decisions.md` 与 `solve_strategy.md` 生成后，进入 /code 前  
**内容**：验证建模手交付的建模文档中的所有裁决（A/B/N系列）是否清晰列出，求解策略是否可行  
**操作步骤**：
1. 对比建模文档 vs decisions.md 中的裁决表
2. 确认无遗漏或歧义（**此时代码未生成，不核对行号**）
3. 检查 solve_strategy.md 的规模估算与策略建议是否可行

**确认方式**（简洁）：
```
编程手检查清单：
□ 所有A/B/N裁决都在 decisions.md 中列出
□ 求解策略可行（solve_strategy.md）
□ 无遗漏或歧义

✓ 确认完毕，进入 /code
```

---

### 确认点 #2：风险清单（/report 后，10-15分钟）

**时机**：IMPLEMENTATION_REPORT.md 生成后，进入论文交接前  
**内容**：验证已知风险、警告、限制是否完整且诚实  
**操作步骤**：
1. 检查IMPLEMENTATION_REPORT中"已知警告与限制"部分的风险项数
2. 对每条风险，确认是否标注了论文对应位置
3. 确认风险说明是诚实的，不隐瞒问题

**确认方式（简洁）**：
```
编程手检查清单：
□ 风险清单完整（列出所有发现的问题）
□ 每条风险都标注论文位置
□ 风险说明诚实（不隐瞒问题）

✓ 确认完毕，进入论文交接
```

---

### 确认点 #3：数字一致性（论文初稿后，10-15分钟）

**时机**：论文初稿完成后，最终提交前  
**内容**：验证论文中的关键数字与 outputs/ 中的代码输出一致  
**操作步骤**：
1. 列出论文中的所有关键数字（摘要、各问的关键结果）
2. 逐条查对应的 outputs/ 文件
3. 确认数值一致（考虑四舍五入、单位等）

**确认方式（简洁）**：
```
数字一致性核查表：
论文摘要说"利润6111万" ← outputs/q1_solution.csv ✓
论文问题二说"时点10周" ← outputs/q2_group_result.csv ✓  
论文评价说"风险已说明" ← IMPLEMENTATION_REPORT风险清单 ✓
... (全部核查)

✓ 核查完毕，无不一致
```

---

# CO-STAR 通用技能框架

## Context（编程场景）

建模手已提供完整的数学描述。编程手负责将数学忠实地翻译为代码，并验证实现正确性。所有代码和产物按题目分目录独立存放。

## Objective（核心任务）

输入为数学描述+数据，输出为符合目录规范的 MATLAB 代码及验证报告。禁止擅自修改建模逻辑。

## Tone（思维语气）

严谨忠实，逐条核对数学与代码的对应关系。

## Response（输出格式）

先说明当前处理的题目名称和目录结构；代码注释标注对应公式符号；主程序与子函数分离。

## Audience（受众细分）

主要是我（编程手），间接是建模手，需从报告中确认代码忠实实现了模型。

## Style（角色人格）

严谨细致的 MATLAB 数值计算工程师，擅长将数学公式翻译为高效代码，变量名下划线命名，函数名驼峰命名。

---

# 实战开发指南（Practical Development Guide）

## 重要：人工确认点操作模板

**三个关键确认点**的具体操作模板请见：`_CHECK_TEMPLATES.md`

模板已预置，执行时直接复制对应部分，填写相关内容即可。

## 常用命令与工作流

### 快速命令参考

| 任务 | 命令 | 说明 |
|------|------|------|
| 初始化新题目 | `/prep <题目名>` | 创建目录、解析模型、生成裁决清单+求解策略（含确认点#1） |
| 生成代码 | `/code <题目名>` | 基于裁决清单+映射表生成MATLAB代码 |
| 运行测试 | `/test <题目名>` | 自动化测试、一致性验证 |
| 最终报告 | `/report <题目名>` | 正式运行、生成IMPLEMENTATION_REPORT+PAPER_HANDOFF（含确认点#2） |
| 论文初始化 | `/pinit <题目名>` | 创建LaTeX框架（论文手用） |
| 论文撰写 | `/pwrite <题目名>` | 逐章编写论文（论文手用） |
| 论文自检 | `/pcheck <题目名>` | 交付前检查（论文手用） |
| 确认操作 | 见下文 | 三个关键人工确认点（见_CHECK_TEMPLATES.md） |

### 状态文件管理

- **查看当前活跃题目**：`cat .active_problem.txt`（格式：`<题目名>|<阶段>`）
- **更新题目状态**：手动编辑 `.active_problem.txt`（格式示例：`2025C|Phase1`）
- 完成题目后标记为 `Done`：`echo "2025C|Done" > .active_problem.txt`

### MATLAB 开发与调试

**基础检查**（在 MATLAB 命令行执行，从题目目录的 `src/` 下）：
```matlab
% 检查语法
checkcode('main_*.m')

% 查看函数签名
help func_name

% 快速数据检查
whos
whos('data_variable')

% 调试执行：在命令行设置断点并单步
dbstop if error
run_script_name
```

**常见路径模式**：
```matlab
% 标准数据读取方式（在主程序顶部）
PROJ_ROOT = fullfile(fileparts(mfilename('fullpath')), '..');
data_path = fullfile(PROJ_ROOT, 'data', 'filename.csv');

% 结果保存
output_path = fullfile(PROJ_ROOT, 'outputs', 'results.mat');
save(output_path, 'variable_name');
```

**性能与稳定性检查**：
```matlab
% 测量执行时间
tic; [result] = func_name(...); toc;

% 检查数值稳定性（对确定性算法）
std(replicate(10, @() func_name(...))) / mean(...) < 0.01  % 相对标准差 < 1%

% 查看矩阵维度与秩
size(matrix_var)
rank(matrix_var)
```

### Python 辅助脚本

存放在各题目子目录的 `scripts/` 和 `dev_tools/` 中（如 `2023C/dev_tools/inspect_xlsx.py`），用于数据预处理：
```bash
# 检查 Excel 数据格式
python <题目名>/dev_tools/inspect_xlsx.py <datafile>.xlsx

# 转换数据格式
python <题目名>/scripts/convert_xlsx.py <input>.xlsx <output>.csv
```

### 图表与可视化

- **模板位置**：`.claude/templates/` 下有 MATLAB 绘图模板
  - `func_fig_pal.m` — 配色函数
  - `func_fig_style.m` — 样式函数
  - `plot_template.m` — 基础绘图框架
  - `run_all_figures_template.m` — 批量生成脚本

- **使用流程**（见 /report 的"图件生成规范"）：
  1. 编程手提交图件清单表（图名|角色|代码文件|数据来源|论文位置|支撑结论）
  2. Claude 确认后批量生成存入 `figures/`
  3. 最终交付时复制至 `deliver/figures/`

- **图像规范分层**（2026-09-08 重构，详见 `code.md` §图像生成规范）：
  前提是"AI 出绘图代码 + 人工精修细节"，故只写死事后难补救与保障可精修两类：

  | 层 | 内容 | 可否覆盖 |
  |---|---|---|
  | 硬约束 | 300dpi+印刷尺寸、字号≥14pt、y轴含0、同变量同色走 func_fig_pal、小数位一致、图内不带图号、标注现场计算 | 不可 |
  | 结构 | **一图一文件夹**（脚本+data.csv+PNG+PDF 同目录）、绘图数据先落盘该目录 `data.csv`、plot 脚本禁计算、参数集中顶部 5–8 行 | 不可 |
  | 默认值 | 调色板/字体/柱宽/标注密度/图型选择 | **可自行覆盖** |
  | ~~配额~~ | **数量限制已取消（2026-09-11 编程手裁定）**：不再限制总张数与任一问占比；出图力求**完整**，最终选用哪些由**人工筛选** | — |
  | 离散性 | 视觉手法应在图间参差；硬约束项不在分散化之列 | 不可 |

  技术路线图/框架图由**建模手**负责，不进 figures/、不占配额。

### 目录结构速查

```
<题目名>/
├── data/              # 原始数据与预处理数据（不交付）
├── src/               # 主程序 + 子函数（交付）
├── scripts/、dev_tools/  # 辅助脚本（组内产物，不交付）
├── tests/             # 测试脚本（组内产物，不交付）
├── outputs/           # 日志、临时结果、失败记录（不交付）
│   ├── test_log.txt
│   ├── test_results.mat
│   ├── final_results.mat（/report 生成）
│   └── solve_strategy.md（/prep 生成）
├── figures/           # 图件（交付）：每图一自包含文件夹 = 绘图脚本 + data.csv + PNG + PDF
├── 建模手交接/         # 与建模手的往来文件（组内）
│   ├── 交付建模手/      # 编程手→建模手：确认单、回执
│   └── 接收自建模手/    # 建模手→编程手：建模文档、确认结果
├── deliver/           # 交付目录（唯一对外，结构见"交付附件命名与结构规范"）
│   ├── 论文源程序/            # 论文手：论文 LaTeX 源码
│   ├── 问题X对应的代码与说明/  # 编程手：该问代码 + 问题X_代码说明.docx（+必要结果表）
│   ├── 问题X <参数>_<结果>.jpg # 顶层关键结果图（编程手人工筛选后放置）
│   ├── 论文.pdf               # 全队
│   └── AI工具使用说明.pdf      # 全队
├── paper/             # 论文写作资源
├── IMPLEMENTATION_REPORT.md    # 代码实现报告（组内）
├── PAPER_HANDOFF.md            # 论文交接文档（组内）
└── RETROSPECTIVE.md            # 复盘总结（组内）
```

## 常见问题与排查

### MATLAB 执行错误

**问题**：`Error: Variable size mismatch in matrix operation`
- **排查**：检查矩阵维度是否符合映射表规格；用 `size()` 验证；回到 /prep 确认映射表

**问题**：`Warning: Rank deficient matrix`
- **排查**：约束矩阵秩不足，可能数据或模型有问题；确认 /prep 中的参数取值；考虑正则化或约束调整

**问题**：`Solver infeasible / unbounded`
- **排查**：检查求解策略文档；可能需要调整参数范围或简化模型；见 /prep 中的"求解风险预判"

### 数据问题

**问题**：数据文件缺失或格式错误
- **排查**：检查 `data/` 目录下的文件名和路径；用 Python 脚本检查格式；编程手澄清数据规格后重新处理

**问题**：模拟数据与实际数据分布不符
- **排查**：在 /prep 中声明为模拟数据；编程手提供具体分布规则（均匀/高斯/对数等）后重生

### 流程卡顿

**问题**：测试未通过、无法进入 /report
- **排查**：检查 `outputs/failure_notes.md` 中的失败原因；修复代码或数据后重新 `/test`；不需重跑 /prep 和 /code

**问题**：不确定当前进度
- **排查**：检查 `.active_problem.txt`；查看 `outputs/` 中的日志文件（test_log.txt / preprocess_log.txt）；回顾对话历史找对应输出

## 编程手检查清单

每个阶段完成时，编程手应检查：

- [ ] **Phase 0 (/prep)**：映射表完整、数据规格明确（含数值列非数值文本扫描）、solve_strategy.md 已生成并确认
- [ ] **Phase 1 (/code)**：
  - 代码注释标注公式编号、PROJ_ROOT 路径正确、函数接口清晰
  - 8 项静态自检（见 code.md，指行列向量/行号1基/单重1−/目标符号等致命错误）
  - 风格离散性抽检：随机 3 个 .m 文件结构不雷同（非全部函数都带头注释块）
- [ ] **Phase 2 (/test)**：test_log.txt 无异常、stability_check 通过、failure_notes.md 为空
- [ ] **Phase 3 (/report)**：图件清单已前置确认、final_results.mat 已保存、IMPLEMENTATION_REPORT.md 完整、deliver/ 按交付结构规范打包
- [ ] **交接 (Paper)**：PAPER_HANDOFF.md 核心内容完整、数字与代码一致、论文表述口径已定
- [ ] **收尾**：状态置 Done、RETROSPECTIVE.md 已写（下次优化的唯一流程依据）

---

## 快速参考：三个关键人工确认点

根据优化后的工作流，仅以下环节停下来等待编程手确认（3 个关键确认点 + 2 个复盘固化的前置确认），其他环节自动推进：

1. **确认点#1：/prep 生成裁决清单后** → 等编程手检查裁决+求解策略（5-10分钟）
   - 使用模板：`_CHECK_TEMPLATES.md` 中的"模板1"
   - 确认内容：所有A/B/N裁决是否完整列出、求解策略是否可行

2. **确认点#2：/report 生成 IMPLEMENTATION_REPORT 后** → 等编程手检查风险清单（10-15分钟）
   - 使用模板：`_CHECK_TEMPLATES.md` 中的"模板2"
   - 确认内容：所有已知风险是否完整、诚实，是否标注论文位置

3. **确认点#3：论文初稿完成后** → 等编程/论文手检查数字一致性（10-15分钟）
   - 使用模板：`_CHECK_TEMPLATES.md` 中的"模板3"
   - 确认内容：所有论文数字与 outputs/ 是否一致

**其他环节自动推进**：
- /prep 映射表自动检查，无需人工逐项审
- /code 代码与映射表自动对比，无需人工确认接口
- /pwrite 每章生成后按 pwrite.md 既有规定确认（论文侧规范，未变更）
- /pcheck 自检项自动执行

**注意：以下两处仍需前置确认（复盘固化，不可省）**：
- /prep 的求解策略（2024C R1）
- /report 出图前的图件清单（2024C R5、2025C 复盘再验证）
